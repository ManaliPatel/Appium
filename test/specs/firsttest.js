"use strict"
var expect = require('chai').expect;
var assert = require('assert');

describe('user logs in', function () {
  it('creates new portfolio', function () {
    iOS.url("http://google.com")
    iOS.setValue('input[title="Search"]',"hello Manali")
    iOS.keys("Enter")
    iOS.pause(4000)
    });
});
